#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
int top(int,int);

int main(){
	printf("%d",top(1,2));
	return 0;
}
int top(int a,int b){
	return a+b;
}
